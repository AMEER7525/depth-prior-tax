[project]
name = "depth-prior-3dgs"
version = "0.1.0"
description = "When do monocular-depth priors trade appearance for geometry in sparse-view 3D Gaussian Splatting?"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
