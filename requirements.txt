# Base dependencies -- install everywhere, including the Mac.
# Everything here runs on CPU/MPS: data loading, splits, depth corruption,
# metrics, and the analysis notebook. No CUDA required.
torch
torchvision
numpy
scipy
pyyaml
opencv-python
imageio
lpips
matplotlib
pandas
tqdm
pytest
